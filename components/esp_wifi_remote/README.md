Place the `esp_wifi_remote` component here. Example:

git clone <esp_wifi_remote_repo_url> esp_wifi_remote

Replace `<esp_wifi_remote_repo_url>` with the upstream repository URL.

Ensure the component includes an `idf_component.yml` or CMakeLists as required by ESP-IDF.
