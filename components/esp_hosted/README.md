Place the `esp_hosted` component here. Example:

git clone <esp_hosted_repo_url> esp_hosted

Replace `<esp_hosted_repo_url>` with the upstream repository URL.

Ensure the component includes an `idf_component.yml` or CMakeLists as required by ESP-IDF.
